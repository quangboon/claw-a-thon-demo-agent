# Secrets — never bake into the image (injected via AgentBase credential at runtime)
.env
.greennode.json
.agentbase/

# Dev / VCS / docs
.git/
.venv/
**/__pycache__/
*.pyc
plans/
docs/
web/node_modules/
web/dist/
# UI is rebuilt in the Docker `web` stage; don't ship the local build
backend/static/

# Transient runtime state
backend/review_queue.jsonl
backend/corrections.jsonl
backend/data/_test_*
